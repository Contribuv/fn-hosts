# Hosts管理器 v1.0.44

> 飞牛NAS hosts 文件可视化管理工具，由豪子制作  

---

## 功能特性

-   **查看** — 以表格形式展示 `/etc/hosts` 所有记录（行号、IP、主机名、注释）
-   **新增 / 编辑** — 单条添加或批量导入，支持 IPv4 和 IPv6
-   **删除** — 带二次确认的安全删除
-   **搜索** — 按 IP 或域名实时过滤
-   **备份与还原** — 一键备份当前 hosts，随时从历史备份恢复
-   **刷新 DNS 缓存** — 修改后即时生效
-   **亮色 / 暗色模式** — 自动跟随 fnOS 系统主题，支持实时切换
-   **管理员权限检测** — 非管理员用户友好提示
-   **响应式布局** — 适配移动端和桌面端

---

## v1.0.44 更新内容

-   亮模式主背景改为纯白（`#fff`），更加干净明亮
-   重新适配亮模式文字、边框、悬停色层次，白色背景下对比度更清晰
-   白底卡片增加微弱阴影区分卡片与背景
-   滚动条颜色跟随主题

---

## 安装要求

| 项目 | 说明 |
|------|------|
| 系统 | 飞牛 NAS（fnOS ≥ 0.9.27） |
| 运行时 | Python 3.11 |
| 权限 | root 安装 |
| 端口 | 5080 |

---

## 安装方法

1. 下载 `fnnas.hosts.fpk`
2. 飞牛 NAS → 应用中心 → 手动安装 → 选择 fpk 文件
3. 安装完成后在桌面打开「Hosts管理器」

---

## 使用说明

-   **新增记录**：点击「新增记录」，填写 IP 和主机名，多个主机名用空格分隔
-   **批量导入**：点击「批量模式」，每行粘贴 `IP 主机名 #注释` 格式的记录
-   **编辑**：在表格中点击对应行的「编辑」按钮
-   **备份**：点击「备份」按钮，自动备份到 `/etc/hosts.bak.*`
-   **还原**：点击「还原」按钮，选择历史备份文件恢复
-   **刷新缓存**：修改 hosts 后点击「刷新缓存」使 DNS 立即生效

---

## 反馈

> 飞牛社区：[https://club.fnnas.com](https://club.fnnas.com)  
> 作者：豪子  
